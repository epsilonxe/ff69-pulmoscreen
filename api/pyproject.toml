[project]
name = "pulmoscreen-api"
version = "0.1.0"
description = "FastAPI service serving the PulmoScreen ELM lung-cancer risk model (FF69)"
license = { text = "MIT" }
authors = [{ name = "Ratthaprom Promkam" }]
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "numpy>=1.26",
    "pydantic>=2.7",
]

[tool.uv]
package = false
